---
- name: Install nginx with Virtual Hosts
  hosts: webservers
  remote_user: runner
  become: true
  gather_facts: no

  vars:
    sites:
    - "etis.com"
    - "fizfak.com"
    - "mehmat.ru"

  roles:
  - nginx-vhosts
